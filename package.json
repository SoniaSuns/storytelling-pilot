{
  "name": "hci-diary-study",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cp index.dev.html index.html && vite",
    "build": "cp index.dev.html index.html && vite build && cp docs/index.html index.html && rm -rf assets && cp -r docs/assets assets && touch .nojekyll docs/.nojekyll",
    "preview": "vite preview --outDir docs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
